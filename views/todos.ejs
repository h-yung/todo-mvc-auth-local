<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <!-- added Date().toDateString() to specify only the day/month/year -->
    <h1>Todos</h1>

    <% if (lastActiveDate) { %>
        <h2>Welcome back, <%= user.userName %>!</h2>
    <% } else { %>   
        <h2>Welcome, <%= user.userName %>!</h2>
    <% } %>
    <p>Today is <%= new Date().toDateString();%>.</p>
    <% if (lastActiveDate) { %>
    <p>You last logged in on <%= lastActiveDate.toDateString() %>.</p>
    <% } %>

    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <div class="todo-item-title"><%= el.title %></div>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                <span class='del'> Delete </span>
                <a href="/edit/<%= el._id %>" class="edit">
                    <span>EDIT</span>
                    </a>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" name="title" value="title">
        <textarea rows="6" cols="45" name="todoItem">what's to do?</textarea>
        <!-- <input type="text" placeholder="Enter Todo Item" name='todoItem'> -->
        <input type="submit" value="add todo">
    </form>

    <a href="/logout">Logout</a>

    <script src="js/main.js"></script>
</body>
</html>